# curl is needed at runtime for cloudflared download in server.js (startChinaTunnel).
# cloudflared itself is downloaded by server.js at startup if not already present.
[phases.setup]
nixPkgs = ["curl"]

[phases.install]
cmds = ["npm install --ignore-scripts"]
