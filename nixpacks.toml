# cloudflared is downloaded at runtime by server.js (startChinaTunnel) if not
# found in PATH. Runtime download to /tmp avoids all build-time exclusion issues.
[phases.install]
cmds = ["npm install --ignore-scripts"]
